# pod.yaml

apiVersion: v1
kind: Pod
metadata:
  name: fresh-app
  labels:
    app: fresh-app
spec:
  containers:
  - name: fresh-app
    image: BillTestContainerRegistry.azurecr.io/fresh-app:latest
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
      